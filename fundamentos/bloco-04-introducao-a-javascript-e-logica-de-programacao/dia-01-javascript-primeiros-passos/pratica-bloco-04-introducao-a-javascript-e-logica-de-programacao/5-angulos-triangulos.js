const a = 30;
const b = 60;
const c = 90;

if (a + b + c == 180 && a > 0 && b > 0 && c > 0) {
    console.log ("Você possui um triangulo válido.")
} else {
    console.log ("Você não possui um triângulo válido.")
}